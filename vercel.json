{
  "buildCommand": "npm install --prefix server && npm install --include=dev --prefix client && npm run build --prefix client",
  "outputDirectory": "client/dist",
  "functions": {
    "api/server.js": {
      "memory": 1024,
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/server?path=$1" }
  ]
}
